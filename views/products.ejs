<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% products.forEach((val, key) => { %>
        <input type="button" onclick="location.href='<%= '/products/' + val %>'" value= <%= val %> >
    <% }); %>
  </body>
</html>